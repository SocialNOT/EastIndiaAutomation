{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "A potential customer lead captured from the website.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the lead."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the lead."
        },
        "company": {
          "type": "string",
          "description": "The company the lead works for."
        },
        "message": {
          "type": "string",
          "description": "An optional message from the lead."
        },
        "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "The timestamp when the lead was created."
        }
      },
      "required": ["name", "email", "company", "createdAt"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/leads/{leadId}": {
      "schema": {
        "$ref": "#/entities/Lead"
      },
      "description": "Stores individual lead documents."
    }
  }
}
